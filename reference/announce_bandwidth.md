# Tell the user which bandwidth was chosen for them

Tell the user which bandwidth was chosen for them

## Usage

``` r
announce_bandwidth(h, cv_fun)
```

## Arguments

- h:

  The chosen bandwidth.

- cv_fun:

  Name of the cross-validation function to point at.

## Value

`h`, invisibly.
