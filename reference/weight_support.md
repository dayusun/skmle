# The support of a weight, with a conservative fallback

The support of a weight, with a conservative fallback

## Usage

``` r
weight_support(weight)
```

## Arguments

- weight:

  A weight function.

## Value

Length-2 numeric.
