% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_async_data}
\alias{sim_async_data}
\title{Simulate asynchronous longitudinal data}
\usage{
sim_async_data(
  n,
  beta = c(0.5, 1.5),
  lambda_y = 5,
  lambda_x = 5,
  link = c("identity", "log", "logistic"),
  x_cov = function(s, t) exp(-abs(s - t)),
  e_cov = function(s, t) 2^(-abs(s - t))
)
}
\arguments{
\item{n}{Number of subjects.}

\item{beta}{Either a numeric vector \code{c(intercept, slopes)} or a function of a
numeric time vector returning a matrix with \code{length(beta(t))} columns and
one row per time.}

\item{lambda_y, lambda_x}{Poisson rates for the response and covariate
observation streams on \verb{(0, 1)}. Each subject is forced to have at least one
observation of each.}

\item{link}{One of \code{"identity"}, \code{"log"} or \code{"logistic"}, matching
\code{\link[=kee_async]{kee_async()}}. Under \code{"logistic"} the response is Bernoulli and no error
process is added; under \code{"log"} the response is Poisson.}

\item{x_cov, e_cov}{Covariance functions of two times, vectorised, for the
covariate and error processes.}
}
\value{
A list of two tibbles on different time grids:
\describe{
\item{y}{\code{id}, \code{time}, \code{y} -- one row per response occasion.}
\item{x}{\code{id}, \code{time}, and one column per covariate coordinate -- named
\code{x} when there is a single covariate, \code{x1}, \code{x2}, ... otherwise. One row
per covariate occasion, on a time grid independent of \code{y}'s.}
}
}
\description{
Draw a sparse longitudinal response and a sparse longitudinal covariate whose
observation times are \strong{independent}, so the two are never seen together.
This is the design of Cao, Zeng and Fine (2015), Section 4: response times and
covariate times are independent Poisson streams on \eqn{(0, 1)}, the covariate
is a mean-zero Gaussian process, and the error is a second, independent
Gaussian process.
}
\details{
The default covariate covariance \eqn{\exp(-|s - t|)} is the Ornstein-
Uhlenbeck covariance. Its diagonal has a kink, so it is only one-sided
differentiable, which is exactly the case Cao, Zeng and Fine's Section 6
leaves open. Supply a smooth
\code{x_cov}, for instance \code{function(s, t) exp(-(s - t)^2)}, to generate data
inside their stated assumptions instead.

\code{beta} may be a numeric vector, giving time-invariant coefficients suitable
for \code{\link[=kee_async]{kee_async()}}, or a function of time returning one row per supplied time,
giving a coefficient curve suitable for \code{\link[=kee_async_td]{kee_async_td()}}. In both cases the
first element is the intercept and the remaining \code{p} elements multiply the \code{p}
covariate coordinates.
}
\examples{
set.seed(1)
d <- sim_async_data(n = 50)
str(d)

# The two tables share `id` and nothing else; their `time` grids are
# independent draws, which is what makes the data asynchronous.
head(d$y, 3)
head(d$x, 3)

# A coefficient curve, for kee_async_td().
d2 <- sim_async_data(n = 50, beta = function(tt) cbind(0.5, 1 + tt))
}
\references{
Cao, H., Zeng, D. and Fine, J. P. (2015). Regression analysis of sparse
asynchronous longitudinal data. \emph{Journal of the Royal Statistical
Society, Series B} 77, 755-776.
}
