% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{confint.kee_td}
\alias{confint.kee_td}
\title{Wald confidence intervals for a coefficient curve}
\usage{
\method{confint}{kee_td}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{kee_td} object.}

\item{parm}{Coefficients to report, by name or position. Defaults to all.}

\item{level}{Confidence level.}

\item{...}{Unused.}
}
\value{
A data frame with one row per (target time, coefficient) and columns
\code{time}, \code{term}, \code{estimate}, \code{se}, and the two interval endpoints.
}
\description{
Pointwise Wald intervals at each target time. They are pointwise, not
simultaneous, and are not corrected for smoothing bias, so they cover
\eqn{E\hat\beta(t)} rather than \eqn{\beta(t)}; see \code{\link[=kee_async_td]{kee_async_td()}}.
}
\details{
For \code{skmle} and \code{kee} fits no method is needed: \code{\link[stats:confint.default]{stats::confint.default()}}
works once \code{\link[=vcov]{vcov()}} is available.
}
\examples{
set.seed(1)
d <- sim_async_data(n = 200, beta = function(tt) cbind(0.5, 1 + tt))
fit <- kee_async_td(y ~ x,
  data_y = d$y, data_x = d$x, id = id, time = time,
  times = c(0.3, 0.5, 0.7), h = 0.3
)
confint(fit)
}
