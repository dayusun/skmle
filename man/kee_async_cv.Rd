% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kee_async.R
\name{kee_async_cv}
\alias{kee_async_cv}
\alias{print.cv.kee_async}
\title{Choose the bandwidth for an asynchronous longitudinal fit}
\usage{
kee_async_cv(
  data_y,
  data_x,
  formula,
  id,
  time,
  h_grid = NULL,
  n_h = 10L,
  K = 5L,
  one_sided = FALSE,
  link = c("identity", "log", "logistic"),
  intercept = TRUE,
  maxit = 50L,
  tol = 1e-08,
  seed = NULL,
  quiet = FALSE
)

\method{print}{cv.kee_async}(x, ...)
}
\arguments{
\item{data_y}{Data frame of response occasions: one row per time the
response was recorded. First argument, so the function pipes.}

\item{data_x}{Data frame of covariate occasions: one row per time the
covariates were recorded. Its time grid need not overlap \code{data_y}'s at all.}

\item{formula}{A two-sided formula, \code{y ~ x1 + x2}. \strong{The left-hand side is
looked up in \code{data_y} and the right-hand side in \code{data_x}}, which is the
one unusual thing about this interface and follows from the data being in
two tables: there is no single frame holding both.}

\item{id}{Subject identifier naming a column present in \strong{both} tables.
Give it unquoted (\code{id = subject}), as a string (\code{id = "subject"}), or
embraced (\code{id = {{ col }}}) when calling from inside another function.
Non-numeric identifiers are allowed.}

\item{time}{Observation time, naming a column present in \strong{both} tables.
Accepts the same three forms as \code{id}.}

\item{h_grid}{Optional numeric vector of candidate bandwidths. When \code{NULL} a
grid is generated from the observation times; see Details.}

\item{n_h}{Number of candidates to generate when \code{h_grid} is \code{NULL}.}

\item{K}{Number of folds. Reduced to the number of subjects if larger.}

\item{one_sided}{Logical. \code{FALSE} (default) is the full two-sided kernel of
the paper; \code{TRUE} is the half kernel, admitting only covariate observations
strictly before the response.}

\item{link}{One of \code{"identity"} (closed form), \code{"log"} or \code{"logistic"}
(Newton-Raphson).}

\item{intercept}{Logical, include an intercept.}

\item{maxit, tol}{Newton-Raphson controls, ignored when \code{link = "identity"}.}

\item{seed}{Optional integer seed for the subject-to-fold assignment. Pass it
(or call \code{\link[=set.seed]{set.seed()}} first) to make the selection reproducible.}

\item{quiet}{Logical; suppress progress output.}

\item{x}{A \code{cv.kee_async} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cv.kee_async}:
\describe{
\item{h_cv}{The selected bandwidth.}
\item{fit}{The \code{\link[=kee_async]{kee_async()}} fit refitted on all subjects at \code{h_cv}.}
\item{cv_results}{Data frame of \code{h}, the mean held-out loss \code{cvloss}, and
\code{nfold_used}, the number of folds that produced a usable fit.}
\item{h_grid, fold_id, seed, call}{Selection metadata.}
}
}
\description{
K-fold cross-validation over \strong{subjects} for \code{\link[=kee_async]{kee_async()}}. For each
candidate bandwidth the estimator is fitted on the training subjects and
scored on the held-out subjects by the kernel-weighted squared error of the
fitted mean,
\deqn{ \frac{\sum_{i \in \mathrm{test}} \sum_j \sum_k W_h(T_{ij} - S_{ik})
       [Y_i(T_{ij}) - g\{X_i(S_{ik})^\top \hat\beta\}]^2}
      {\sum_{i \in \mathrm{test}} \sum_j \sum_k W_h(T_{ij} - S_{ik})}.}
}
\section{How the folds are formed}{
Folds are formed over subjects, never over rows. Rows within a subject come
from one trajectory, so splitting by row would put a subject on both sides of
the split and leak the answer into the held-out score.
}

\section{Why the loss is normalised}{
Dividing by the total weight is what makes bandwidths comparable. A larger
\code{h} admits more pairs and so accumulates a larger unnormalised error whatever
the quality of the fit; without the denominator the criterion would select
the smallest candidate every time.
}

\section{Why squared error and not a likelihood}{
The estimator solves an estimating equation, not a likelihood, so there is no
held-out log-likelihood to evaluate. The same weighted squared error on the
response scale is used for all three links, applied after \code{link}.
}

\section{The default grid}{
When \code{h_grid} is \code{NULL} the grid is log-spaced over
\eqn{[2 (Q_3 - Q_1) n^{-0.7},\, 2 (Q_3 - Q_1) n^{-0.3}]}, where
\eqn{Q_1, Q_3} are quartiles of the pooled observation times of both tables
and \eqn{n} is the number of subjects. This is the rule used by the reference
implementation of Cao, Zeng and Fine's method, and it adapts to the scale of
\code{time} on its own.

Always look at \code{cv_results}. If the minimum sits at an endpoint of the grid a
warning is issued, and the grid should be widened by hand: the selected value
is then a boundary artefact rather than a minimum.
}

\examples{
set.seed(1)
d <- sim_async_data(n = 150, beta = c(0.5, 1.5))
cv <- d$y |>
  kee_async_cv(d$x, y ~ x,
    id = id, time = time,
    h_grid = c(0.15, 0.25, 0.40), K = 3, seed = 1, quiet = TRUE
  )
cv
cv$cv_results
}
\references{
Cao, H., Zeng, D. and Fine, J. P. (2015). Regression analysis of sparse
asynchronous longitudinal data. \emph{Journal of the Royal Statistical
Society, Series B} 77, 755-776.
}
\seealso{
\code{\link[=kee_async]{kee_async()}}
}
