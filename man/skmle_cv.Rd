% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skmle_cv.R
\name{skmle_cv}
\alias{skmle_cv}
\title{Bandwidth Selection for skmle Model using Cross-Validation}
\usage{
skmle_cv(
  formula,
  data,
  id,
  obs_times,
  s,
  K = 5,
  h_grid = NULL,
  n_h = 10,
  nknots = 3,
  norder = 3,
  lq_nodes = 64,
  maxeval = 10000,
  xtol_rel = 1e-06,
  quiet = FALSE
)
}
\arguments{
\item{formula}{A formula object, with the response on the left of a \code{~} operator, and the terms on the right. The response must be a survival object as returned by the \code{Surv} function. The model must include at least one covariate (intercept-only formulas are unsupported).}

\item{data}{A data.frame in which to interpret the variables named in the formula, or in the \code{id} argument.}

\item{id}{A vector identifying subjects in the data. Values are coerced to integer
codes internally (e.g. via \code{factor}) so non-integer identifiers are allowed.}

\item{obs_times}{The observation times for the subjects.}

\item{s}{The s parameter for the Box-Cox transformation function. \code{s = 0} corresponds to the proportional hazards model, and \code{s = 1} corresponds to the additive hazards model.}

\item{K}{Number of folds for cross-validation. Default is 5.}

\item{h_grid}{Optional numeric vector of bandwidth candidates. If NULL, a grid is generated automatically based on observation spacings.}

\item{n_h}{Number of bandwidths to generate if \code{h_grid} is NULL. Default is 10.}

\item{nknots}{The number of knots for the B-splines used to approximate the baseline hazard function. Default is 3.}

\item{norder}{The order of the B-splines. Default is 3 (cubic splines).}

\item{lq_nodes}{The number of Legendre-Gauss quadrature nodes for the integral of the baseline hazard. Default is 64.}

\item{maxeval}{Maximum number of evaluations for nlopt optimization. Default is 10000.}

\item{xtol_rel}{Relative tolerance for nlopt optimization. Default is 1e-6.}

\item{quiet}{Whether to suppress progress messages. Default is FALSE.}
}
\value{
A list of class \code{cv.skmle} containing the optimal bandwidth \code{h}, the estimated model with optimal bandwidth (\code{fit}), a data.frame of \code{cv_results}, and \code{h_grid}.
}
\description{
Performs K-fold cross-validation to select the optimal bandwidth \code{h} for the \code{skmle} model.
}
