% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skmle_cv.R
\name{skmle_cv}
\alias{skmle_cv}
\title{Select the Bandwidth by Cross-Validation}
\usage{
skmle_cv(
  formula,
  data,
  id,
  obs_times,
  s,
  K = 5,
  h_grid = NULL,
  n_h = 10,
  nknots = 3,
  norder = 3,
  lq_nodes = 64,
  maxeval = 10000,
  xtol_rel = 1e-06,
  quiet = FALSE
)
}
\arguments{
\item{formula}{A model formula. The left-hand side must be a \code{survival::Surv()}
response and the right-hand side must contain at least one covariate.}

\item{data}{Data frame containing the variables used in \code{formula}, \code{id}, and
\code{obs_times}.}

\item{id}{Subject identifier. Non-numeric identifiers are allowed and are internally
converted to integer subject codes.}

\item{obs_times}{Longitudinal observation times aligned row-wise with \code{data}.}

\item{s}{Box-Cox transformation parameter. \code{s = 0} gives the proportional hazards
model and \code{s = 1} gives the additive hazards model.}

\item{K}{Number of folds.}

\item{h_grid}{Optional numeric vector of candidate bandwidth values. If \code{NULL},
a grid is generated automatically from the observed time gaps.}

\item{n_h}{Number of candidate bandwidths to generate when \code{h_grid} is \code{NULL}.}

\item{nknots}{Number of interior knots used in the sieve approximation of the
baseline component.}

\item{norder}{Order parameter supplied to the high-level interface for the spline
approximation.}

\item{lq_nodes}{Number of Legendre-Gauss quadrature nodes used in numerical
integration.}

\item{maxeval}{Maximum number of optimizer evaluations.}

\item{xtol_rel}{Relative convergence tolerance passed to the optimizer.}

\item{quiet}{Logical; if \code{TRUE}, suppress progress output.}
}
\value{
An object of class \code{cv.skmle} with components:
\itemize{
\item \code{h_cv}: selected bandwidth,
\item \code{fit}: \code{skmle} fit refit on the full data,
\item \code{cv_results}: data frame of candidate bandwidths and CV losses,
\item \code{h_grid}: bandwidth grid used in the search.
}
}
\description{
Perform K-fold cross-validation to select the kernel bandwidth for \code{skmle()}.
}
\details{
\code{skmle_cv()} splits subjects, not rows, across folds. This is the appropriate
unit for cross-validation because multiple rows belong to the same subject in the
long-format data structure.

After choosing the bandwidth with the smallest average validation loss, the
function refits \code{skmle()} on the full data set using the selected value.
}
\examples{
\donttest{
library(survival)

set.seed(123)
dat <- sim_skmle_data(
  n = 60,
  mu = function(tt) 8 * (0.75 + (0.5 - tt)^2),
  mu_bar = 8,
  alpha = function(tt) 0.5 * 0.75 + 0.75 * (tt * (1 - sin(2 * pi * (tt - 0.25)))),
  beta = c(1, -0.5),
  s = 0,
  cen = 0.7
)

cv_fit <- skmle_cv(
  Surv(X, delta) ~ covariates,
  data = dat,
  id = id,
  obs_times = obs_times,
  s = 0,
  K = 3,
  h_grid = c(0.3, 0.4, 0.5),
  quiet = TRUE
)

cv_fit$h_cv
cv_fit$cv_results
summary(cv_fit$fit)
}

}
