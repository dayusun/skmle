% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{epan_weight_spec}
\alias{epan_weight_spec}
\title{The Epanechnikov kernel described as a polynomial}
\usage{
epan_weight_spec(one_sided = TRUE)
}
\arguments{
\item{one_sided}{Logical; \code{TRUE} for the half kernel.}
}
\value{
A list with \code{coef}, \code{a}, \code{b} and \code{mirror}.
}
\description{
The cross-validation loop rebuilds the weights for every candidate bandwidth
inside C++ and cannot call an R function to do it, so the weight travels as
four plain values instead: the coefficients of \eqn{\sum_j c_j u^j}, the
support, and whether the shape functions are \eqn{|u|^j} rather than
\eqn{u^j}. For \eqn{0.75(1 - u^2)} the coefficients are \code{c(0.75, 0, -0.75)}
and the support is \verb{[0, 1]} for the half kernel, \verb{[-1, 1]} for the full one.
\code{calc_kerfun()} in \code{src/skmle_cpp.cpp} reads them back.
}
\details{
This is the second description of a kernel the package already has in
\code{\link[=kernel_weights]{kernel_weights()}}, so \code{test-cv-weightspec.R} holds the two against each
other. Two descriptions that nothing compares is how they drift apart.
}
\keyword{internal}
