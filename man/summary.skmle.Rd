% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skmle.R
\name{summary.skmle}
\alias{summary.skmle}
\title{Summary for skmle object}
\usage{
\method{summary}{skmle}(object, ...)
}
\arguments{
\item{object}{An object of class \code{skmle}.}

\item{...}{Further arguments passed to or from other methods.}
}
\value{
An object of class \code{summary.skmle} containing the call,
a coefficient table with estimates, standard errors, z-statistics
and p-values, the log-likelihood, convergence status, and the sample
size \code{n}.
}
\description{
Summary for skmle object
}
\examples{
\donttest{
library(survival)

set.seed(123)
dat <- sim_skmle_data(
  n = 60,
  mu = function(tt) 8 * (0.75 + (0.5 - tt)^2),
  mu_bar = 8,
  alpha = function(tt) 0.5 * 0.75 + 0.75 * (tt * (1 - sin(2 * pi * (tt - 0.25)))),
  beta = c(1, -0.5),
  s = 0,
  cen = 0.7
)

fit <- skmle(Surv(X, delta) ~ covariates, data = dat, id = id,
             obs_times = obs_times, s = 0, h = 0.5, nknots = 3)
summary(fit)
}
}
