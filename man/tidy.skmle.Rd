% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{tidy.skmle}
\alias{tidy.skmle}
\alias{tidy.kee}
\alias{tidy.kee_td}
\title{Summarise a fit as a tibble}
\usage{
\method{tidy}{skmle}(x, conf.int = FALSE, conf.level = 0.95, ...)

\method{tidy}{kee}(x, conf.int = FALSE, conf.level = 0.95, ...)

\method{tidy}{kee_td}(x, conf.int = FALSE, conf.level = 0.95, ...)
}
\arguments{
\item{x}{A fitted \code{skmle}, \code{kee} or \code{kee_td} object.}

\item{conf.int}{Logical, add \code{conf.low} and \code{conf.high} columns.}

\item{conf.level}{Confidence level for those columns.}

\item{...}{Unused.}
}
\value{
A tibble with columns \code{term}, \code{estimate}, \code{std.error}, \code{statistic}
and \code{p.value}, preceded by \code{time} for a \code{kee_td} fit, and followed by
\code{conf.low} and \code{conf.high} when \code{conf.int = TRUE}.
}
\description{
One row per coefficient for \code{skmle} and \code{kee} fits, and one row per
(target time, coefficient) for \code{kee_td}, so a coefficient curve goes
straight into \code{ggplot2} without reshaping.
}
\examples{
set.seed(1)
d <- sim_async_data(n = 150)
fit <- kee_async(d$y, d$x, y ~ x, id = id, time = time, h = 0.3)
tidy(fit, conf.int = TRUE)
}
