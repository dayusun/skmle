% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{glance.skmle}
\alias{glance.skmle}
\alias{glance.kee}
\alias{glance.kee_td}
\title{One-row summary of a fit}
\usage{
\method{glance}{skmle}(x, ...)

\method{glance}{kee}(x, ...)

\method{glance}{kee_td}(x, ...)
}
\arguments{
\item{x}{A fitted \code{skmle}, \code{kee} or \code{kee_td} object.}

\item{...}{Unused.}
}
\value{
A one-row tibble. \code{nobs} is the number of \strong{subjects}, which is the
unit the asymptotics are in. No \code{AIC} or \code{logLik} column is reported: the
criterion these estimators optimise is a kernel-weighted pseudo-likelihood,
so an information criterion built from it would not mean what it usually
means.
}
\description{
One-row summary of a fit
}
\examples{
set.seed(1)
d <- sim_async_data(n = 150)
fit <- kee_async(d$y, d$x, y ~ x, id = id, time = time, h = 0.3)
glance(fit)
}
