% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{vcov.skmle}
\alias{vcov.skmle}
\alias{vcov.kee}
\alias{vcov.kee_td}
\title{Extract the covariance matrix of a fitted model}
\usage{
\method{vcov}{skmle}(object, ...)

\method{vcov}{kee}(object, ...)

\method{vcov}{kee_td}(object, time = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{skmle}, \code{kee} or \code{kee_td} object.}

\item{...}{Unused.}

\item{time}{For a \code{kee_td} fit, the target time whose covariance matrix is
wanted. Matched to the nearest fitted target time. When \code{NULL} the full
list of matrices is returned, one per target time.}
}
\value{
For \code{skmle} and \code{kee}, the estimated covariance matrix of the
regression coefficients. For \code{kee_td}, see \code{time}.
}
\description{
Extract the covariance matrix of a fitted model
}
\examples{
set.seed(1)
d <- sim_async_data(n = 120)
fit <- kee_async(d$y, d$x, y ~ x, id = id, time = time, h = 0.3)
vcov(fit)
}
