% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_skmle_data}
\alias{sim_skmle_data}
\title{Simulate data for Transformed Hazards Models}
\usage{
sim_skmle_data(n, mu, mu_bar, alpha, beta, s, cen, nstep = 20)
}
\arguments{
\item{n}{The number of subjects to simulate data for.}

\item{mu}{A function specifying the intensity rate for the Non-Homogeneous Poisson Process generating the observation times.}

\item{mu_bar}{An upper bound for the intensity function \code{mu}.}

\item{alpha}{A function specifying the baseline hazard component.}

\item{beta}{A numeric vector of true regression coefficients for the generated covariates (length 2 in this implementation). The function currently only supports two covariates; supplying a vector of another length raises an error.}

\item{s}{The Box-Cox transformation parameter (0 for proportional hazards, 1 for additive hazards).}

\item{cen}{The maximum follow-up or censoring time parameter.}

\item{nstep}{Number of steps for generating the piecewise constant longitudinal covariate process. Default is 20.}
}
\value{
A tibble (data frame) in long format containing the following columns:
\item{id}{Subject identifier.}
\item{X}{The observed survival or censoring time.}
\item{delta}{Event indicator (TRUE if event occurred, FALSE if censored).}
\item{covariates}{A two-column matrix of the observed covariates. Column 1 is a time-varying process, and column 2 is a constant (binary) indicator for each subject.}
\item{obs_times}{The exact times at which the covariates were observed.}
\item{censoring}{The randomly generated right-censoring time for the subject.}
}
\description{
Simulates right-censored survival data with sparsely and
intermittently observed longitudinal covariates under a transformed hazards model.
}
\details{
\code{sim_skmle_data} generates survival data and sparsely observed longitudinal covariates.
The covariate process is generated as a piece-wise constant step function. Survival times
are simulated utilizing the specified transformed hazards model and the \code{gaussquad} numerical
integration technique to invert the cumulative hazard. Observation times for the longitudinal
covariates are generated using a Non-Homogeneous Poisson Process.
}
\examples{
\dontrun{
library(dplyr)
# Simulate a dataset of 200 subjects under the proportional hazards model (s = 0)
set.seed(123)
# the returned data frame contains a matrix column `covariates` with two
# columns: a time-varying component and a constant indicator
sim_data <- sim_skmle_data(
    n = 200,
    mu = function(tt) 8 * (0.75 + (0.5 - tt)^2),
    mu_bar = 8,
    alpha = function(tt) 0.5 * 0.75 + 0.75 * (tt * (1 - sin(2 * pi * (tt - 0.25)))),
    beta = c(1, -0.5),
    s = 0,
    cen = 0.7
)
head(sim_data)
}

}
