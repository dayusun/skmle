% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kee_async.R
\name{plot.kee_td}
\alias{plot.kee_td}
\title{Plot estimated coefficient curves}
\usage{
\method{plot}{kee_td}(x, which = seq_len(ncol(x$coefficients)), level = 0.95, ...)
}
\arguments{
\item{x}{A \code{kee_td} object from \code{\link[=kee_async_td]{kee_async_td()}}.}

\item{which}{Integer or character vector selecting coefficients to draw.
Defaults to all.}

\item{level}{Confidence level for the pointwise bands.}

\item{...}{Passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
\code{x}, invisibly. Called for the plot.
}
\description{
Draws \eqn{\hat\beta_j(t)} against \code{t} with pointwise 95\% Wald bands, one
panel per coefficient. The bands are pointwise and are not corrected for
smoothing bias, so they cover \eqn{E\hat\beta(t)}, not \eqn{\beta(t)}.
}
\examples{
set.seed(1)
d <- sim_async_data(n = 200, beta = function(tt) cbind(0.5, 1 + tt))
fit <- kee_async_td(d$y$id, d$y$time, d$y$y, d$x$id, d$x$time, d$x$x,
  times = seq(0.2, 0.8, by = 0.05), h = 0.3
)
plot(fit)
}
