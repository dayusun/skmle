% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skmle.R
\name{skmle}
\alias{skmle}
\title{Fit a Transformed Hazards Model by SMKLE}
\usage{
skmle(
  formula,
  data,
  id,
  obs_times,
  s = 0,
  h = NULL,
  nknots = 3,
  lq_nodes = 64,
  maxeval = 10000,
  xtol_rel = 1e-06,
  one_sided = TRUE
)
}
\arguments{
\item{formula}{A model formula. The left-hand side must be a \code{survival::Surv()}
response and the right-hand side must contain at least one covariate.}

\item{data}{Data frame containing the variables used in \code{formula}, \code{id}, and
\code{obs_times}.}

\item{id}{Subject identifier. Non-numeric identifiers are allowed and are internally
converted to integer subject codes.}

\item{obs_times}{Longitudinal observation times aligned row-wise with \code{data}.
Times may be on any scale; the sieve basis and the cumulative-hazard
quadrature are built on the observed follow-up, so there is no need to
rescale to the unit interval first. \code{h} must be on the same scale.}

\item{s}{Box-Cox transformation parameter, defaulting to \code{0}. \code{s = 0} is the
proportional hazards model, \code{s = 1} is the additive hazards model, and
values in between interpolate. If you do not have a reason to choose
otherwise, the default is the familiar Cox model.}

\item{h}{Positive kernel bandwidth. If omitted, one is read off the
observation times as a rule of thumb and reported in a message. Use
\code{\link[=skmle_cv]{skmle_cv()}} to choose it from the data.}

\item{nknots}{Number of interior knots used in the sieve approximation of the
baseline component. Knots are placed at \code{(1:nknots)/(nknots + 1)}. The
basis is a natural cubic spline (\code{splines::ns}); its order is fixed, which
is why there is no \code{norder} argument.}

\item{lq_nodes}{Number of Legendre-Gauss quadrature nodes used in numerical
integration.}

\item{maxeval}{Maximum number of optimizer evaluations.}

\item{xtol_rel}{Relative convergence tolerance passed to the optimizer.}

\item{one_sided}{Logical. \code{TRUE} (the default) uses a half kernel: only
covariate observations strictly before the event or quadrature time inform
that time, which is the risk-set restriction and the estimator as
published. \code{FALSE} uses a full, two-sided kernel, smoothing the covariate
path from both sides. The switch applies to the risk-set
averages inside the C++ backend as well as to the row weights, so the two
are always consistent.}
}
\value{
An object of class \code{skmle} containing:
\itemize{
\item \code{coefficients}: regression coefficient estimates,
\item \code{var}: estimated variance-covariance matrix for the regression coefficients,
\item \code{gamma}: estimated spline coefficients for the baseline component,
\item \code{loglik}: maximized log-likelihood,
\item \code{convergence}: optimizer status code,
\item model metadata such as \code{n}, \code{s}, \code{h}, and \code{call}.
}
}
\description{
Fit a transformed hazards model for survival data with sparsely and intermittently
observed longitudinal covariates using the sieve maximum kernel-weighted
log-likelihood estimator (SMKLE).
}
\details{
\code{skmle()} is the main model-fitting function in the package. It combines:
\itemize{
\item kernel weighting to handle intermittently observed longitudinal covariates,
\item a sieve approximation for the unknown baseline component, and
\item a C++-backed numerical optimizer for the joint estimation problem.
}

The returned object follows the usual R model pattern: print the fitted coefficients
with \code{print()}, obtain inferential output with \code{summary()}, and visualize the
estimated baseline component with \code{plot()}.
}
\examples{
\donttest{
library(survival)

set.seed(123)
dat <- sim_skmle_data(
  n = 80,
  mu = function(tt) 8 * (0.75 + (0.5 - tt)^2),
  mu_bar = 8,
  alpha = function(tt) 0.5 * 0.75 + 0.75 * (tt * (1 - sin(2 * pi * (tt - 0.25)))),
  beta = c(1, -0.5),
  s = 0,
  cen = 0.7
)

fit <- skmle(
  Surv(X, delta) ~ covariates,
  data = dat,
  id = id,
  obs_times = obs_times,
  s = 0,
  h = 0.5,
  nknots = 3
)

summary(fit)
}

}
\references{
Sun, Dayu, Zhuowei Sun, Xingqiu Zhao, and Hongyuan Cao.
"Kernel Meets Sieve: Transformed Hazards Models with Sparse Longitudinal Covariates."
\emph{Journal of the American Statistical Association} (2025): 1-12.
}
