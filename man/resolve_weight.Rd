% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{resolve_weight}
\alias{resolve_weight}
\title{Resolve the weight argument}
\usage{
resolve_weight(weight, one_sided)
}
\arguments{
\item{weight}{A weight function, or \code{NULL}.}

\item{one_sided}{Logical, used only when \code{weight} is \code{NULL}.}
}
\value{
A weight function.
}
\description{
\code{NULL} means "the Epanechnikov implied by \code{one_sided}", which is what every
call made before \code{weight} existed was asking for. Resolving it here rather
than as a default argument keeps that backward compatibility exact: a
literal default of \code{w_epan_half()} would silently turn
\code{one_sided = FALSE} into a one-sided fit, because the support would still
be \verb{[0, 1]}.
}
\keyword{internal}
