% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{augment.kee_async}
\alias{augment.kee_async}
\title{Add fitted means to the covariate table}
\usage{
\method{augment}{kee_async}(x, data_x, ...)
}
\arguments{
\item{x}{A \code{kee_async} fit.}

\item{data_x}{The covariate table the fit was made from.}

\item{...}{Unused.}
}
\value{
\code{data_x} as a tibble with a \code{.fitted} column added.
}
\description{
Fitted means belong to covariate occasions. The estimating equation
evaluates the link at each observed covariate vector, and the kernel weight
is what ties that vector to a response occasion. So \code{augment()} returns
\code{data_x} with a \code{.fitted} column, \eqn{g(X_i(S_{ik})^\top \hat\beta)}.
}
\details{
There is deliberately no \code{.resid}. A residual needs a response value at
\eqn{S_{ik}}, and asynchronous data has none; any residual reported here
would have to be invented.

The fit does not retain its data, so \code{data_x} must be supplied.
}
\examples{
set.seed(1)
d <- sim_async_data(n = 150)
fit <- kee_async(d$y, d$x, y ~ x, id = id, time = time, h = 0.3)
augment(fit, d$x)
}
